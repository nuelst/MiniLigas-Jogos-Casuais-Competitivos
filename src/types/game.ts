export type GameRanking = {
  rank: number;
  name: string;
  score: number;
  gamesPlayed: number;
  avatar: string;
};

export type GameTab = {
  id: string;
  name: string;
  emoji: string;
  color: string;
};
